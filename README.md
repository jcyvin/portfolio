# Olive Portfolio Hub

A polished Next.js portfolio landing page with dark olive styling and internal sample-site routes.

## Run locally

```bash
npm install
npm run dev
```

## Deploy to Vercel

Push the repo to GitHub and import it in Vercel. The project is already set up as a standard Next.js app, so Vercel will detect it automatically.
